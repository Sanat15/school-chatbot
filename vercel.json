{
  "version": 2,
  "buildCommand": "rm -rf api/backend api/static && cp -r backend api/backend && cd frontend && npm install && npm run build && cp -r dist ../api/static",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ]
}
